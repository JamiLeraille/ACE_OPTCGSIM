# @op/web

Frontend React 18 + TypeScript + Vite + Tailwind CSS 4 (rendu DOM/CSS, pas de canvas ;
animations framer-motion).

- **Phase 1** : deck-builder (catalogue virtualisé, filtres, validation en direct,
  sélecteur d'alt-art, import/export, page de deck partageable).
- **Phases 2+** : table de jeu.

```bash
pnpm --filter @op/web dev
```

Actuellement : coquille avec disclaimer légal (fan-made, non affilié à Bandai).
