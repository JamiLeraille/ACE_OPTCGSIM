# @op/api

API REST Fastify + Zod (comptes, decks, catalogue de cartes). Valide avec les schémas
de `@op/shared` — les mêmes que le deck-builder.

```bash
pnpm --filter @op/api dev   # http://localhost:3001/health
```

Actuellement : squelette avec `/health`. Les routes cartes/decks arrivent en Phase 1.
